# 네이버 클라우드 플랫폼 > Maps > Application의 Client ID (ncpKeyId)
# 콘솔에서 Web Dynamic Map 사용 + 서비스 URL에 http://localhost:5173 등록 필요.
# 발급 후 이 파일을 .env로 복사해 값을 채우고 vite를 재시작하면 지도가 네이버지도로 전환된다.
# (키가 없으면 Leaflet+OSM 폴백으로 동작 — 데모는 그대로 굴러간다)
VITE_NAVER_MAP_KEY_ID=
