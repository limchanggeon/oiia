# 코레일톡(멤버십) 계정 — 실제 예약(좌석 확보)에만 필요. 조회는 계정 없이 동작한다.
# 이 파일을 server/.env로 복사해 값을 채우면 서버 재시작 시 로드된다 (gitignore 대상).
# ⚠️ 절대 client/.env에 넣지 말 것 — VITE_ 변수는 브라우저 번들에 노출된다.
# ⚠️ 이 example 파일에는 실제 값을 넣지 말 것 — 깃에 추적되는 파일이다.
# KORAIL_ID: 코레일 멤버십 번호(10자리) 또는 이메일/휴대전화
KORAIL_ID=
KORAIL_PW=
